version: '3.8'
services:
  db:
    container_name: datarest
    image: postgres:alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: datarest
    ports:
      - "5434:5432"
    networks:
      - postgres-compose-network
  pgadmin:
    container_name: pgadmin4-datarest
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: root
    ports:
      - "15434:80"
    networks:
      - postgres-compose-network
networks:
  postgres-compose-network:
    driver: bridge